streamlit>=1.38
streamlit-authenticator>=0.4  # non utilise par app.py actuellement (login desactive) ; garde pour tests/test_auth.py et une reactivation future
pyyaml>=6.0
python-dotenv>=1.0  # charge la cle du modele depuis le fichier .env (voir .env.exemple)
scikit-learn>=1.3
pandas>=2.0
openpyxl>=3.1
tabulate>=0.9
pytest>=8.0  # pour lancer la suite de tests (dossier tests/)
python-docx>=1.1   # lecture des fichiers Word .docx deposes dans data/source_documents/
python-pptx>=1.0   # lecture des fichiers PowerPoint .ppt/.pptx deposes dans data/source_documents/
pdfplumber>=0.11   # repli pour lire les PDF si pdftotext (poppler) n'est pas installe

# Un seul des deux suffit, selon le fournisseur de LLM choisi :
groq>=0.9        # gratuit, rapide (recommande pour demarrer)
anthropic>=0.34  # si tu preferes utiliser une cle API Claude
